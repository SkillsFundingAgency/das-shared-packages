trigger:
  batch: true
  branches:
    include:
      - "DASD-10915-SFA-DAS-Testing-YAML-Setup"
  paths:
    include:
    - pipeline-templates
    - SFA.DAS.Testing

pr:
  autoCancel: true
  branches:
    include:
      - "DASD-10915-SFA-DAS-Testing-YAML-Setup"
  paths:
    include:
    - pipeline-templates
    - SFA.DAS.Testing

resources:
  repositories:
  - repository: self
  - repository: das-platform-building-blocks
    type: github
    name: SkillsFundingAgency/das-platform-building-blocks
    ref: refs/tags/refs/tags/2.1.0
    endpoint: SkillsFundingAgency

stages:
- stage: Build
  jobs:
  - template: ../pipeline-templates/job/code-build.yml
    parameters:
      WorkingDirectory: SFA.DAS.Testing
      DotNetStandardPackagesToPack: SFA.DAS.Testing/SFA.DAS.Testing/SFA.DAS.Testing.csproj
      SonarCloudProjectKey: SkillsFundingAgency_das-shared-packages_SFA.DAS.Testing
- template: azure-pipelines-templates/deploy/stage/nuget-publish.yml@das-platform-building-blocks